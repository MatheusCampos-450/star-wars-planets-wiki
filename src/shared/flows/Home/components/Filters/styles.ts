import { tv } from 'tailwind-variants';

export const filterStyles = tv({
  slots: {
    filterContainer:
      'gap-xs flex h-[2.5rem] w-full items-center justify-center',
    filterTextContainer: 'gap-xxxs flex items-center justify-center',
  },
});
