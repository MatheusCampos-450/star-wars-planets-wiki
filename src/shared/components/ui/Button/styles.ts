import { tv } from 'tailwind-variants';

export const buttonStyles = tv({
  base: 'bg-red font-lato flex h-[2.5rem] w-full cursor-pointer items-center justify-center rounded-[.3125rem] text-sm font-bold text-white',
});
